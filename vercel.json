{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://finance-analyzer-api.onrender.com/api/:path*"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
